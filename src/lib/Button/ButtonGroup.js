import styled from "styled-components";
import View from "../View";

const ButtonGroup = styled(View).attrs({
  // className: ({ ...props }) => classNames(
  //
  // ),
})``;

export default ButtonGroup;
